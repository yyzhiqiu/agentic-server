# Memory Notes

本文件用于沉淀会话记忆、长期记忆与上下文压缩策略。

## 基本原则

- 会话消息、摘要和长期记忆应区分职责，不混用存储边界。
- 短期上下文优先服务当前对话，长期记忆只保留可复用的稳定事实。
- 上下文压缩应说明触发条件、保留字段与丢弃风险。

## 工程约束

- 记忆读写逻辑应通过 `graph/` 节点或 `services/` 协调，不直接散落在 API 层。
- 涉及用户数据持久化时，应明确归属关系、生命周期和清理策略。
