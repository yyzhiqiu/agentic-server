# Troubleshooting Notes

本文件用于沉淀本地开发与部署过程中的常见问题排查路径。

## 常见排查顺序

- 先检查环境变量、依赖安装与端口占用。
- 再检查数据库、Redis、对象存储与外部模型服务是否可用。
- 如果是 Compose 环境，优先核对容器健康检查、日志和网络连通性。

## 协作建议

- 排障记录应尽量回写到 `docs/` 或 `skills/operations/`，避免经验只停留在对话里。
- 涉及目录结构、启动命令或部署入口的修复，完成后要同步更新 README 与文档。
