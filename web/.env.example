# 前端应用元信息
# 用于浏览器界面展示的应用名称。
VITE_APP_NAME=Agent Platform

# 后端 API 目标地址
# 本地开发时指向 FastAPI 服务的基础地址。
# Agent 列表与默认 Agent 选择由后端 /v1/agents 返回的元信息驱动。
VITE_API_BASE_URL=http://localhost:8000
# 需要与后端 API_PREFIX 保持一致，前端请求才能命中正确路由前缀。
VITE_API_PREFIX=/v1
